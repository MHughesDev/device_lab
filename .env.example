# .env.example — copy to .env for local development. Never commit real secrets.
#
# Day 1 setup:
#   cp .env.example .env
#   python scripts/generate_secrets.py   # fills SECRET_KEY, POSTGRES_PASSWORD, FIRST_SUPERUSER_PASSWORD
#
# This template ships with a full-stack FastAPI + React app under apps/api and
# apps/web. The variables below are consumed by:
#   - docker compose (compose.yml / compose.override.yml)
#   - the backend (apps/api/app/core/config.py via pydantic-settings)
#   - the frontend (apps/web reads VITE_API_URL at build time)

# ----- Domain / hosts -----
# Traefik routes traffic by host. For local dev, leave as localhost.
DOMAIN=localhost
# Used by the backend to generate links in emails to the frontend.
FRONTEND_HOST=http://localhost:5173

# Environment: local, staging, production
ENVIRONMENT=local

PROJECT_NAME="Template Full-Stack App"
STACK_NAME=template-fullstack

# ----- Backend -----
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173,https://localhost,https://localhost:5173"
SECRET_KEY=changethis
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=changethis

# ----- Emails (optional) -----
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=info@example.com
SMTP_TLS=True
SMTP_SSL=False
SMTP_PORT=587

# ----- Postgres -----
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changethis

# ----- Observability -----
SENTRY_DSN=

# ----- Docker images (override for your registry) -----
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend

# ----- Optional capabilities (template systemization) -----
# AI / RAG (enable ai optional deps: `pip install -e .[ai]`)
# AI_ENABLED=false
# CHROMA_HOST=localhost
# CHROMA_PORT=8000

# MCP (Model Context Protocol) dev server
# MCP_ENABLED=true

# ----- Security middleware -----
# Rate limiting (per-IP token bucket)
RATE_LIMIT_ENABLED=true
RATE_LIMIT_ANON=60      # requests per minute for unauthenticated IPs
RATE_LIMIT_AUTH=300     # requests per minute for authenticated IPs

# Security headers (HSTS, X-Frame-Options, CSP, etc.)
SECURITY_HEADERS_ENABLED=true
CSP_POLICY=             # leave empty to use the strict built-in default

# Auth audit log (JSON lines to stdout for 401/403 and auth routes)
AUDIT_LOG_ENABLED=true
